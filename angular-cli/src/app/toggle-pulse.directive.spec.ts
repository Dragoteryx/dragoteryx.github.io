import { TogglePulseDirective } from './toggle-pulse.directive';

describe('TogglePulseDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglePulseDirective();
    expect(directive).toBeTruthy();
  });
});
