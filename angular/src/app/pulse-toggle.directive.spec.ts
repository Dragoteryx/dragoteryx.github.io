import { PulseToggleDirective } from './pulse-toggle.directive';

describe('PulseToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new PulseToggleDirective();
    expect(directive).toBeTruthy();
  });
});
